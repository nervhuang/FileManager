# 變更紀錄

## 2026-08-27
- 修正拖曳一批檔案之後，選取範圍會被改成另一段
  - 為了保住整組選取，壓在已選取項目上時不能呼叫 `QTreeView.mousePressEvent`，於是 Qt 內部的框選錨點 `pressedPosition` 停留在**上一次真正壓下**的位置。`drag.exec_()` 回來之後，後續的滑鼠移動落到 `super().mouseMoveEvent()`，Qt 就進入 `DragSelectingState`，從那個過期錨點重新框選——游標往哪邊走，選取就往哪邊改
  - 修法是自行啟動拖曳之後吃掉後續的移動事件，直到下一次壓下或所有按鍵放開
  - 這個缺陷在 2026-08-13 讓「左鍵只在檔名欄選取」整個功能被回退（commit 1e64fcc）。當時的判斷是「合成事件測不到，問題出在 `drag.exec_()` 的 modal loop」——那個判斷是錯的。offscreen 就重現得出來，缺的只是**拖曳結束之後再送一次滑鼠移動**這一步
- 工具列加上作者／團體面板的切換鈕，位置在「新增資料夾」與更新檢查器之間
  - 作者面板原本只有「檢視 → 顯示作者清單面板」與 Ctrl+Shift+A 兩個入口。關掉之後要找回來得先記得它在選單的哪一層——這和更新檢查器當初把入口放上工具列的理由是同一個：只靠選單叫得出來的面板，等於藏起來了
  - 兩顆切換鈕並列成一組（作者／團體 → 更新檢查器），中間不夾別的按鈕，因為它們做的是同一件事：開關側邊面板
  - 圖示沿用作者面板既有的人形群像，去掉「新增團體」那顆的綠色加號徽章——一顆是「建一筆」，一顆是「開面板」，徽章正是兩者唯一該有的差別
  - 按鈕不做 checkable：面板在不在畫面上就是狀態回饋。選單項目的勾選已經有一條同步路徑，再加一條只是多一個會腐化的地方
  - 新加的按鈕不需要額外登記就會跟著字型縮放（遞迴走訪整棵 widget 樹），圖示尺寸也自動被既有的 64×64 遍歷測試涵蓋——這正是那兩條規則當初改成自動化的目的

## 2026-08-26
- 修正左鍵拖放無聲失敗，檔案既沒搬也沒複製
  - 拖放來源出自 `QUrl.toLocalFile()`，它在 Windows 回的是正斜線（`D:/a/b.txt`）。`SHFileOperationW` 不吃這種路徑：實測直接回錯誤 183，檔案留在原地
  - 主視窗的 `_perform_file_op` 有把路徑正規化，`views._apply_drop_operation` 沒有。兩者其餘部分逐行相同——是複製出去之後各自演化的重複程式碼，而少了修正的那一份，正是每次左鍵拖放都會跑的那一份
  - 發現時機是把檔案操作域拆出來之前的盤點：兩份實作互相 diff，唯一差別就是少了 `os.path.normpath`
  - 迴歸測試用 Qt 形式的正斜線路徑拖一個檔案，斷言它真的被搬走。測試同時把 `QMessageBox.warning` 換掉——它是 modal，不換的話失敗情境會卡住整輪測試，而不是回報失敗

## 2026-08-24
- `config.ini` 壞掉時要能撐住，而不是拒絕啟動
  - `configparser` 的 `fallback=` 只在鍵**不存在**時生效。鍵存在但值的型別不對——手動編輯過、寫入寫到一半——會讓 `getint` / `getboolean` 拋例外，`load_config` 中斷，程式完全打不開
  - 有七個鍵暴露在這個風險下：字型大小、排除功能開關、兩個面板的顯示狀態與寬度、目前分頁索引。走 JSON 與逗號解析的鍵原本就有防護，只有型別 getter 沒有
  - 設定檔壞掉的代價應該是使用者損失設定，不是損失整個程式。現在每個鍵各自在解析失敗時回退預設值，互不牽連
  - 由「拆設定層之前先寫的特徵化測試」找到的——設定檔損壞這個情境從來沒有被跑過
- 工具列圖示全部改為自繪，不再留任何 Qt 系統圖示
  - 同一條工具列上並存三種視覺語言：沒有填色的線稿（剪下、複製、貼上、更名）、填色加描邊的圖畫（資料夾、版面、重新整理）、Windows 系統圖示（上一頁、下一頁、新增資料夾、垃圾桶）。並排看一眼就知道它們不是同一套
  - 四個系統圖示改成既有語言的自繪版本——填色、深色描邊、帶高光：與「上一層」綠色箭頭同族的綠色上／下一頁箭頭、掛上作者面板既有「新增」綠色加號徽章的資料夾、一個金屬垃圾桶
  - 四個線稿圖示改繪成實心形狀：兩張疊起來的紙、握把是藍色圓環的剪刀、夾著紙的藍色剪貼板、一支壓在底線上的鉛筆
  - 剪刀刀刃用一般墨色而不是金屬灰；旁邊都是飽和色時，灰色看起來像褪色
  - 圖示繪製從 `widgets.py` 以及 `initUI` 裡的兩個閉包搬進 `app/icons.py`，調色盤也放在那裡。`file_manager.py` 從 2807 行降到 2599 行，`widgets.py` 從 917 行降到 858 行。驗法是實際算繪：搬動前後八個圖示的像素雜湊完全相同
- 更新檢查器剩下的兩個工具列圖示改為自繪，不再用 `QStyle` 的
  - 工具列上並存兩種視覺語言：兩個手繪扁平圖示，旁邊是兩個 Windows 系統圖示（一份反光的文件、一支擬真掃把）。它們通過尺寸規則——都提供 128×128——但除此之外沒有一處相稱
  - `make_detail_icon()` 是一個瀏覽器視窗，裡面 2×2 縮圖格。畫瀏覽器而不是單純的清單，因為這個按鈕開的是外部分頁，不是在面板裡展開東西——輪廓要在工具提示之前就把這件事說完
  - `make_reset_icon()` 是一疊記錄，右下掛紅色圓形箭頭徽章。用紅色而不是綠色：它清掉的是既有結果，和程式其他地方那個綠色的重新整理是不同性質的動作，顏色要在使用者按下去之前就分開
  - 徽章刻意畫大。64px 圖示裡只有 26px 時，圓環、缺口與箭頭會糊成一個白色的「C」，不再讀得出是循環箭頭
  - 圓環與箭頭是同一條封閉路徑，和 `widgets.make_refresh_icon()` 一樣；拆成兩個形狀畫的話，不管怎麼對齊接合處都會看到一條縫
- 字型縮放改成套用到整個程式，而不是一份手工維護的 widget 清單
  - 把字級調到 21pt 時，選單列、狀態列、三條工具列與主視窗本身都還停在 12pt；只有清單、分頁列、麵包屑與兩個面板跟著變
  - 病因不是漏了幾個 widget，是程式碼的形態：`_apply_font_size` 用逐一列舉的方式實作橫切關注點，於是每加一個面板都得記得回來登記自己。作者面板漏過一次（完全不理 Ctrl+=），更新檢查器是第二次補登記
  - 新的 `app/font_scaling.py` 走遍整棵 widget 樹，套用的是**差值**而不是絕對字級，所以刻意做出來的偏移得以保留——工具列按鈕比內文大兩級以平衡圖示與文字，檢查器的計數列大一級，它的日誌則小一級並用等寬字
  - 先全部量完，再全部套用。`setFont` 會往下傳播到還沒自己設過字型的子 widget，邊走邊改的話，往下每一層讀到的都已經是新值，差值會一層層疊上去——在 12pt 的樹上實測，葉節點被壓到 6pt 下限
  - `QApplication.setFont` 刻意不碰：那是全域可變狀態，會讓這個操作變成順序相依，而這次改動的第一版就讓兩支測試互相污染。對話框本來就自己套字型，因為 Qt 在頂層視窗邊界會停止傳播
  - 三支測試：全部 176 個 widget 都跟著變、先前漏掉的選單／狀態列／工具列逐一點名、以及 +9 / 6pt / 40pt 來回之後縮放可還原
- 更新檢查器的停止圖示改為自繪，不再用 `QStyle` 的
  - `SP_MediaStop` 最大只提供到 32×32，Qt 又不會放大，所以在 64px 的工具列裡它只有旁邊五個鄰居的一半大——這和 `SP_BrowserReload` 是同一個問題，而當時留下的解法（`widgets.make_refresh_icon`）沒有被重用
  - 新的 `checker.icons.make_stop_icon()`：一個紅色圓角方形，寬度 40/64，與重新整理圓環的直徑相同，兩者並排時份量相等。畫方形而不是圓形，是為了讓輪廓和旁邊那個圓環分得開。高光約佔寬度四成——橫貫整個寬度會讀成減號，那比較像「移除」而不是「停止」
  - 新測試會走遍每一個工具列按鈕，只要有圖示的 `actualSize(64,64)` 不是 64×64 就失敗。`availableSizes()` 回報的是來源圖檔的尺寸而不是算繪出來的尺寸，這就是它一直沒被發現的原因
- 修好兩支一直在失敗卻沒人發現的測試腳本，並讓測試與開發者個人設定隔離
  - `test_search_click_realapp.py` 從 2026-06-13 起就跑不動了：它傳的是路徑字串清單，而 `update_search_results` 早已改用 `SearchResult` tuple。它防的是 proxy 對應錯亂導致點擊時崩潰，也就是說那道防護已經缺席兩個月
  - `test_move_no_sync_refresh.py` 每一條斷言都過，卻以 1 結束——繁體中文 Windows 主控台是 cp950，編不出它結果行裡的那個勾號
  - 兩支現在都把 `FILEMANAGER_HOME` 指向空的暫存目錄。它們會真的建一個 `FileManager`，那會讀到開發者自己的 `config.ini`；這台機器的 `[Exclude]` 涵蓋整個 `C:\`，所以建在 `C:\...\Temp` 底下的測試檔案被正確地濾光了，看起來卻像測試壞掉
- 更新檢查器限定中文與日文，其餘一律抑制
  - 只有語言集合為空（日文原作）或完全落在 {japanese, chinese} 之內的畫廊才會通報；出現白名單以外的值就抑制（`VERDICT_SUPPRESSED`——存起來但不顯示）
  - 直接讀站上 `language:` 命名空間的值，不再比對關鍵字字典。`_LANGUAGE_KEYWORDS` 只列了 14 種語言，站上有三十幾種，而 `is_wanted_language()` 把「認不出來」當成「日文原作」——於是荷蘭文和烏克蘭文都溜了進來，白名單實際上是黑名單。改讀命名空間之後它才真的是白名單：沒列到的一律預設排除。這同時也擋掉了 `female:` / `other:` 標籤裡剛好含有語言名稱造成的誤判
  - 判定從嚴不從寬。站上的語言標籤常常落後：日文標籤還掛著，標題卻已經寫明是英文翻譯，寬鬆規則會因為 `japanese` 在白名單裡就放行。以 5 位作者 × 最新 25 筆＝103 個畫廊實測，兩種規則的結果完全一致（各排除 27 筆，26%）；擔心的「多語言誤殺」在標籤頁上並不會發生
  - 抑制不再取決於本機是否已經收藏。舊規則只讓已收藏的標題閉嘴，理由是還沒收藏的作品仍應被看見；但實務上，只存在韓文或英文翻譯版本的書本來就不會被收藏。代價是這些書從此完全不會出現
  - 語言標記帶 `lang:` 前綴，與品質標記分開。前綴不是裝飾：站上的語言值不由我們控制，`language:ukrainian` 就算不在任何字典裡，也必須仍然看得出來**它是一種語言**
  - 修正 `refresh_verdicts()` 會弄丟標記。重新評估時只重新解析標題，把站上標籤提供的語言與無修正資訊丟掉了——於是因 `language:korean` 被排除的書退回「沒有語言標記＝日文原作」而重新出現，自己撤銷了自己的排除；無修正也有同樣的問題，版本升級會退回成已收藏
  - `FIRST_RUN_LIMIT` 從 10 調到 25。標籤頁一次回 25 筆，gdata 的 `API_BATCH` 也是 25，所以 10 和 25 都是兩個請求。語言限制會吃掉大約四分之一的額度，基準線太小的話剩下的只有個位數
  - 日誌窗格改為顯示「每位作者」的排除筆數。用每位作者而不是總數，因為靜默的過濾器很危險：語言限制如果壞了，症狀是每位作者都回報「沒有更新」，而看不到任何原因。一行「排除 25、新增 0」才分得出是過濾器壞了還是這位作者真的很安靜
  - 新增 重設掃描紀錄 工具列按鈕，清掉發現結果與狀態，但保留使用者標記的忽略／已下載旗標——規則改過之後，舊的資料列救不回來，只能重新抓
  - `scripts/test_checker_language.py` 是純函式測試：不連網、不用帳密、不用 Qt。站上那種扁平標籤字串的格式，已對照公開的 gdata API 驗證過

## 2026-08-23
- 新增更新檢查器：把 exhentai 上的新發布拿來和本機藏書比對
  - 用 `authors.db` 裡的 `english_name` 組出 `artist:` / `group:` 標籤查詢 exhentai，再把回來的中繼資料與本機檔名比對，分成四組：全新、版本升級、可能已有、已有
  - 比對用的是 E-Hentai API 的 `title_jpn`。本機檔名是日文，站上的英文標題是羅馬拼音，拿那個去比會全部落空
  - 判定分三級而不是兩級。本機檔名常有錯字，所以同一本書可能只差一個假名——而同一位作者的另一本書也可能只差一個假名，分數一模一樣。沒有任何單一門檻分得開它們，所以中間那一段留給人判斷
  - 語言用白名單（中文翻譯與日文原作）而不是黑名單：對三位作者跑一輪就冒出英文、韓文、西班牙文與葡萄牙文——黑名單列不完。多語言版本與同一本書的重複上傳會依核心標題聚合成一列，否則同一本書會被通報三次
  - 中間那條工具列上新增圖示；右側面板顯示四組的計數與新發布清單。雙擊會開一個本機 Web UI（只綁 127.0.0.1、隨機埠、每次請求一組 token），顯示縮圖牆
  - 縮圖快取在本機並從本機供應，不是直接盜連 `s.exhentai.org`
  - 登入憑證放在 `runtime_root()/exhentai.txt`，已列入 gitignore，且絕不寫進日誌、例外訊息或網頁。檔案不存在就是硬錯誤，絕不無聲退回匿名請求
  - `app/checker/` 是自成一體的子套件；`authors_db.py` 完全沒動（它需要的 schema 增補由 `store.py` 施加）
- 更新檢查器加上日誌窗格，並讓它的字型與版面跟隨主視窗
  - 窗格顯示進度列（`%v/%m %p%` 加上目前的作者）、已用與預估剩餘時間，以及每位作者一行。跑完一輪要 25 到 35 分鐘，而狀態列只留得住最後一行，所以原本根本看不到進度、失敗或剩餘時間
  - `scanner.scan_all()` 多了 `on_result(index, total, result)` 回呼；既有的 `progress` 是在一個實體開始**之前**觸發，所以它知道誰在跑，不知道跑出了什麼
  - `QPlainTextEdit` 以 `maximumBlockCount(2000)` 設上限，避免幾百位作者累積成記憶體
  - 新增 `CheckerPanel.apply_font_size()`，由 `FileManager._apply_font_size()` 沿著和 `AuthorsPanel` 相同的路徑呼叫。子 widget 必須逐一重設——計數列比內文大一級、日誌是小一級的等寬字，而明確設過字型的 widget 就不再繼承父層。這正是 Ctrl+= / Ctrl+- 對這個面板毫無反應的原因。日誌的下限是 8pt，好讓主視窗縮到 6pt 時它仍然看得見
  - 四個新的 `[Layout]` 鍵：面板顯示狀態、面板寬度、清單／日誌的分割、清單欄寬。前兩個由主視窗擁有；後兩個封裝在 `CheckerPanel.layout_state()` / `restore_layout()` 裡，主視窗不需要知道這個面板裡有幾個 splitter
  - 還原時會跳過全為零的值：如果設定寫入之前這個面板從未顯示過，隱藏 widget 的尺寸都是 0，照著套用會把清單與日誌都壓成零高度

## 2026-08-22
- 作者與團體新增 `english_name`，用來從站上的查詢反查實體
  - `entities` 新增 `english_name` 欄（單一字串、一對一，不是清單），既有資料庫以 `ALTER TABLE` 補上。它是純中繼資料：不會併進 `search_terms_for`，也不影響本機檔案搜尋結果
  - `fm_authors_list` / `authors-list` 的關鍵字過濾與回傳的實體都涵蓋 `english_name`
  - `fm_authors_upsert` / `authors-upsert` 可以寫入它
  - `fm_match_author` 與 `fm_authors_link` 接受它作為名稱參數（經由 `_resolve_entity`），所以英文名可以解回日文或中文的實體
  - GUI 的新增／編輯對話框多了 英文名稱 欄位，清單的工具提示也會顯示
  - CLI 不需要改：它直接轉呼叫 `hermes_mcp` 的工具函式，新欄位會自動流過去
- 新增 CLI，讓 Hermes 以外的程式也能呼叫同一套功能
  - `app/cli.py` 直接呼叫 `hermes_mcp.py` 的工具函式（`@server.tool()` 裝飾器原樣回傳函式本身），沒有重複任何邏輯，所以行為與授權閘門和 MCP 那一側完全相同
  - 九個指令對應九個 MCP 工具；輸出是 stdout 上的一行 JSON，`ok=false` 時以 1 結束
  - stdout/stderr 固定為 UTF-8。Windows 主控台預設用系統 codepage（繁中環境常是 cp950），遇到日文作者名會拋 `UnicodeEncodeError`——而那正是這支 CLI 的核心資料
- 貼上的團體名稱，括號裡有多位作者時要拆開（以 、 或 , 分隔）
  - `サイクロン (和泉、冷泉)` 原本會變成一位叫 `和泉、冷泉` 的作者。括號內容現在會以頓號與半形／全形逗號拆開，各自成為一位連結的作者。不限數量

## 2026-08-21
- 貼上的 `團體 (作者)` 名稱拆成團體加作者，並建立連結
  - 同人誌清單的慣用寫法（`zero戦 (xxzero)`，或加上方括號的 `[zero戦 (xxzero)]`）貼進名稱欄時，會拆成團體與作者，並走既有的 `linked_names` 路徑寫入，雙向建立連結
  - 只在**新增**模式攔截。編輯既有項目不動它，因為既有名稱裡本來就可能合理地含有括號
  - 取的是字串結尾的最後一組括號；半形、全形，以及外層的方括號都接受
- `linked_names` 改為合併而不是整組取代
  - Hermes 通常一次處理一位作者，對同一個團體分開呼叫 upsert。`_set_links` 會先刪掉該實體所有既有連結再插入，於是後面每一位作者都把前面那些記下的連結洗掉。`_add_links` 現在只補缺的，這次沒列到的既有連結原封不動。要移除連結請用 `fm_authors_link(unlink=true)`
- `EverythingSDK` 改為每執行緒一個實例，避免視窗類別名稱衝突
  - MCP 框架透過 `anyio.to_thread.run_sync` 執行工具呼叫，所以同一個工具連續呼叫可能落在不同的 worker 執行緒上。跨執行緒共用的單例會讓其他執行緒永遠收不到 Everything 的回覆——查詢無聲逾時，回傳空清單。實例現在依執行緒快取，回覆視窗的類別名稱也帶上 `id(self)`，好讓同一個進程裡的第二個實例不會把第一個實例的類別註冊吞掉

## 2026-08-16
- 新增 Hermes 整合：共用的作者／團體清單與遠端檔案搜尋
  - 新增 `authors.db`（SQLite、WAL），作為作者與團體的唯一真實來源
    - 單一實體表，帶 `type`（作者／團體）、別名，以及作者⇄團體的多對多連結
    - 每一次寫入都記進 `changes` 表，附前後快照；刪除是軟刪除，所以 Hermes 犯的任何錯都能從 GUI 還原
  - 新增左側作者面板（`app/authors_panel.py`），可由 檢視 → 顯示作者清單面板（Ctrl+L）切換，回到原本的兩欄版面
    - 團體與其作者的樹狀清單；點一筆會用它的名稱加上所有別名開一個搜尋分頁
    - 可新增／編輯／刪除項目、管理別名與連結，並從 最近變更 對話框還原 Hermes 的變更
    - 面板的顯示狀態與寬度存在 `config.ini` 的 `[Layout]`
  - 新增 MCP stdio server（`app/hermes_mcp.py`），對 Hermes 開放七個工具
    - 檔案搜尋用 `fm_search`、`fm_match_author`、`fm_authors_stats`；清單用 `fm_authors_list` / `_upsert` / `_delete`；驅動 GUI 用 `fm_open_search_tab`
    - 它是獨立進程，所以不論 GUI 開著沒有，清單存取與搜尋都能用；它套用與 GUI 相同的 `[Exclude]` 設定
    - GUI 關著時 `fm_open_search_tab` 回 `gui_not_running`，只有設了 `launch_if_needed` 才會去啟動它
  - 新增 GUI 與 MCP server 之間的本機管線（`app/gui_bridge.py`，`QLocalServer`）
    - Hermes 對清單的寫入會推給 GUI，GUI 立刻更新面板
    - GUI 會先回應管線再去跑搜尋，所以慢查詢不再讓呼叫端誤以為逾時
  - 把搜尋關鍵字解析抽到 `app/search_query.py`，讓 GUI 與 MCP server 產生完全相同的結果（已用 `config.ini` 裡的每一個關鍵字對照舊實作驗證過）
  - 把執行期路徑解析抽到 `app/paths.py`（不依賴 Qt）
- 作者面板的過濾框上方加一條圖示工具列
  - 新增作者／新增團體／編輯／刪除／重新整理／最近變更，以分隔線分組，取代原本放在底部的文字按鈕
  - 圖示與檔案面板工具列同樣是 64px 並帶文字標籤，但這條列保持獨立，因為這個面板可以單獨關掉
  - 圖示用與檔案面板資料夾圖示相同的「填色加描邊」風格，而不是光禿禿的線條；刪除與重新整理沿用檔案面板用的同一組系統圖示
  - 以 `QToolBar` 建構，這樣面板被拉窄時按鈕會收進溢位選單，而不是強迫一個很寬的最小寬度（面板最小寬維持 151px）
  - 沒有選取任何項目時，編輯與刪除是停用的
  - 面板預設寬度改為 660px，也就是六個按鈕剛好排得下的寬度
- 任何 MCP 工具回應之前，都要求 FileManager 程式正在執行
  - 開著程式就是使用者授權的方式；程式關著時每個工具都回 `gui_not_running`，磁碟上與清單裡的東西一概讀不到
  - 這道閘門涵蓋全部九個工具，不只是會碰磁碟的那些——只擋一部分，等於邀請模型去拿另一個工具
  - `fm_open_search_tab` 移除了 `launch_if_needed` 選項：一個能啟動程式的工具，可以自己授予這道閘門存在的目的所要拒絕的存取
- 新增 `fm_search_all`，`fm_search` 的分頁批量版本
  - 欄位與 `fm_search` 相同，另加 `total`、`offset` 與 `has_more`；用 `offset` 一頁一頁翻，直到 `has_more` 為 false。每頁預設 200 筆，上限 2000 筆（約 560KB）
  - `fm_search` 看不到「單次查詢向 Everything 要求的上限」之外的東西，所以一個命中 6989 個檔案的關鍵字只回報 2000。批量工具只為自己的呼叫抬高那個上限，程式本身的搜尋不受影響
  - 新增 `capped` 欄位，回報「Everything 自己的上限也到頂了」，也就是說連 `total` 本身都可能是低估——這和 `has_more` 不同，後者只說明這一頁不完整
- 誠實回報搜尋被截斷
  - `truncated` 原本只反映呼叫端給的 `limit`，所以因內部單次查詢上限而被切掉的搜尋，會宣稱自己是完整的
- 讓 `FILEMANAGER_HOME` 可以覆寫執行期目錄
  - MCP server 從專案的 venv 執行（不是凍結的），資料目錄會解析到專案資料夾，而打包後的 exe 解析到自己那個資料夾——於是兩個進程各讀各寫一份 `authors.db` 與 `config.ini`，Hermes 存的東西在程式裡看不到
  - 在 Hermes 的 `mcp_servers` 設定裡把這個變數指向安裝好的 exe 所在資料夾，兩邊就用同一份檔案
  - MCP server 的說明現在會寫出解析後的資料目錄，所以指錯地方是看得見的，不是無聲的
- 兩個面板的分頁列都加上「所有分頁」清單
  - 開啟方式：對 ＋ 按鈕按右鍵、對分頁列空白處按右鍵，或按 ＋ 旁邊新增的 ˅ 按鈕；對分頁本身按右鍵不做任何事
  - 每一列顯示分頁的完整路徑或搜尋關鍵字，而不是分頁本身放得下的那十個字，過長時從中間省略，好讓磁碟機代號與最後一層資料夾都留著，完整文字放在工具提示
  - 目前分頁打勾並加粗；點一列就切到那個分頁並捲進可視範圍
  - 沒有資料的分頁退回顯示它的標籤（本機 / 新頁籤）
- 分頁的關閉按鈕跟著程式字型大小縮放
  - 分頁本來就會跟著字型長大，但關閉按鈕的尺寸來自樣式的 `PM_TabCloseIndicator*` 度量，所以字型愈大，那個 ✕ 相對就愈小。現在按鈕的尺寸依分頁列的字型度量決定，並在新增分頁或字型改變時重新調整
  - 只調按鈕大小還不夠：原生樣式會在給它的任何矩形裡畫一個置中的固定大小 ✕。現在改由一個 proxy style 依按鈕大小等比繪製 ✕，連同它自己的滑鼠停留與按下回饋
  - proxy style 掛在按鈕上而不是分頁列上，因為 `QWidget::setStyle` 傳不到子 widget——那些關閉按鈕用的是程式的樣式，不是分頁列的
- 讓麵包屑位址列跟隨程式字型大小
  - 只對 `BreadcrumbBar` 設字型不夠：這條列本身、它的路徑按鈕、它的編輯框各自帶著 stylesheet，而 Qt 會把「有樣式的 widget」的字型當成明確設過，於是父層的字型不再往下傳。現在每一個子 widget 都直接指派字型，導覽後重建的路徑按鈕也繼承得到
  - 這條列的高度現在會跟著字型長大，而不是停在 30px 的最小值
  - `_sync_right_header_spacing` 改在這條列改變大小之後才重算；它原本在新字型套用之前就跑了，字級一大就讓搜尋面板的分頁列差 2px 對不齊
- 重新整理圖示改為自繪，不再用 `QStyle` 的
  - `SP_BrowserReload` 只提供 24×24 與 32×32，Qt 又不會放大，所以在 64px 的工具列裡它只有旁邊每個鄰居的一半大——兩條工具列都是
  - 新的 `widgets.make_refresh_icon()` 依要求的尺寸繪製；18 個工具列圖示現在全都回報 64×64
  - 用資料夾圖示的綠色繪製，圓環與箭頭是同一條封閉路徑——拆成兩個形狀畫的話，不管怎麼對齊接合處都會看到一條縫——箭頭尖端落在圓環的中心線上，讓箭頭留在圓內
  - 其他還在用的系統圖示（垃圾桶、資料夾、箭頭）都提供 128×128，從來沒受影響
- 檔案面板工具列改建在 `QToolBar` 上，並放大到與作者面板工具列一致
  - 它原本固定的 `QHBoxLayout` 有 1506px 的最小寬度，這個值會傳播到中間面板，讓視窗小於 1657px 時作者面板的 splitter 握把完全拉不動——兩邊都卡在各自的最小值。最小寬度現在是 94px，握把可以自由移動
  - 按鈕改成文字在圖示下方，兩條工具列形狀一致，而且兩條被釘在同一個高度，不會各自漂移
  - `_sync_right_header_spacing` 改為量工具列的固定高度而不是它的 `sizeHint`，後者小 4px，會讓搜尋面板的分頁列對不齊
- 作者面板的切換快捷鍵移出 `Ctrl+L`，那個組合麵包屑列已經用來聚焦路徑編輯器——同一個序列兩個動作會讓快捷鍵變成有歧義的。改為 `Ctrl+Shift+A`
- 讓「作者與它的團體變成兩筆互不相干的資料」更難發生
  - 新增 `fm_authors_link` 工具，單獨連結（或解除連結）一位作者與一個團體，缺哪一邊就建哪一邊——`fm_authors_upsert` 原本為了加一個關係就得把整筆記錄重送一次
  - `fm_authors_upsert` 現在明確寫出：已知的作者／團體配對必須連同 `linked_names` 一起送
  - 編輯對話框原本在別名／連結欄輸入後沒按 Enter 就按確定，會把打好的字丟掉；現在會先把待處理的文字提交進去
- 作者 群組底下列出**所有**作者，不再只列沒有團體的那些，好讓群組的計數與它顯示的內容相符
- 讓作者面板跟隨程式字型大小
  - `_apply_font_size` 從來沒碰過這個面板，所以它的樹狀清單、過濾框與按鈕都停在預設字級，也不理 Ctrl+= / Ctrl+-（以及啟動時從 `config.ini` 還原的字級）
  - 它的對話框也不繼承：Qt 在頂層視窗邊界會停止字型傳播，所以不管面板是什麼字級，對話框永遠以程式預設值開啟
- 打包時不要再把 `config.ini` 包進去
  - 它會落在 `_internal/` 裡，但程式讀的是 exe 旁邊的 `config.ini`，所以那份包進去的複本從來沒被載入過——它只是把建置機器的搜尋歷史與私人路徑一起發出去
  - 沒有 `config.ini` 的首次啟動本來就支援：程式用內建預設值執行，結束時再把檔案寫出來

## 2026-06-17
- 修正檔案操作卡頓與全形括號的搜尋處理
  - 消除新增／刪除／更名／搬移之後 2 到 3 秒的 UI 凍結
    - 重建結果 model 期間關掉搜尋 proxy 的動態排序（每次 `appendRow` 都重排，在最多 2000 列時是 O(n²)）
    - 只有可能新增命中的操作（貼上／搬移／拖放）才重跑完整的 Everything 查詢；刪除／更名／外部變動改成輕量的「列是否還在」對帳
    - 去掉拖放時觸發的重複同步搜尋刷新
    - 停掉會把檔案面板整個重載的 `setRootPath("")`，改為依賴 `QFileSystemModel` 內建的監看器逐步更新目前顯示的目錄
  - 修正搜尋面板更名時誤報「名稱已存在」——只改大小寫的更名（Windows 的 `os.path.exists` 不分大小寫；正在被改名的那個檔案不再算成衝突）
  - 解讀搜尋關鍵字時，認得全形／中日韓括號裡的文字，以及用連字號分隔的詞
    - 查詢建構器現在也會搜尋去掉括號後的內文（單一 token 直接搜；多個 token 走 Everything 原生的空格 AND，另有 regex 備援）
    - 比對過濾改成 token 子集合判定，讓括號造成的空白不再把有效的命中判掉
    - `extract_keywords`（點檔名自動搜尋）現在認得全形 `（）［］｛｝` 與中日韓的 `【】〔〕「」『』〈〉《》`，不再只認 ASCII 的 `([{`

## 2026-06-14
- 修正檔案面板與搜尋面板的多重選取
  - 檔案面板：啟用 `ExtendedSelection`，可以選取多個檔案（原本只能選一個）
  - 搜尋面板：拖曳多選項目中的其中一個，現在會拖整組選取，而不是塌縮成被點的那一個
    - 按在已選取的項目上時，view 停留在 `NoState`，於是第一次滑鼠移動被當成橡皮筋選取，把多重選取蓋掉了
    - 現在按下時保留選取並手動啟動拖曳；只點不拖仍然會塌縮成被點的那一個

## 2026-06-08
- 分頁拖放改寫成完全自訂的實作
  - 關掉 Qt 原生的可移動拖曳（它的殘影 widget 在捲動途中無法重新定位）
  - 被拖的分頁畫成一個浮動複本，全程跟著游標
  - 界內拖曳時，浮動複本的中心越過鄰居就立刻重排分頁
  - 界外拖曳時，以計時器一次跳過一個隱藏分頁（每步 80 ms）
  - 拖曳期間隱藏被拖分頁的關閉按鈕，避免版面出現殘留
  - 新增 `scroll_index_into_view`，用 `arrowType()` 可靠地找出左／右捲動按鈕

## 2026-06-01
- 改善檔案瀏覽的操作與發版衛生
  - 新增右側面板的水平／垂直版面切換，並依版面各自記住 splitter 方向
  - 新增右側搜尋框的自動搜尋，並改善純關鍵字的比對
  - 新增複製、剪下、貼上、Backspace 上一層導覽，以及新增資料夾後聚焦更名
  - 改善搜尋結果的拖放刷新行為，以及拖曳時的自動捲動
  - 把版控中的 config.ini 重設為淨化過的預設值，供儲存庫與發版建置使用

## 2026-02-01
- 新增工具列，含字型控制、圖示與 UI 修正
  - 工具列橫跨頂部，四個按鈕（放大字型／縮小字型、刪除、內容）
  - 加入動態的 A/a 圖示與 SVG 備援
  - 實作放大／縮小字型的動作，以及狀態列顯示
  - 依選取狀態啟用／停用 刪除 與 內容
  - 各種錯誤修正與除錯訊息清理
