"""檔案操作域：剪貼簿、拖放、原生右鍵選單、回收筒、重新命名。

這個域大部分行為在 CI 上驗不了——Windows shell COM、原生選單、右鍵拖放、
回收筒都需要真實 shell。因此界線要畫得比別的域更用力：能算成純函式的
（路徑判定、衝突判斷、選取解析）就不要混進 shell 呼叫裡，那才是測得到的部分。

`shell.py` 不依賴 Qt：它只認路徑與視窗代號（hwnd），訊息呈現留給呼叫端。
"""
