"""更新檢查器：比對 exhentai 最新發布與本機藏書，找出還沒收的書。

規格與設計決策見專案根目錄的 CHECKER.md。

本套件分兩層：

* 純函式層（`titles`／`matcher`）不依賴 Qt、不碰網路，可獨立測試，
  也方便日後若要搬回獨立專案時整包帶走。
* I/O 層（`fetcher`／`scanner`）負責網路與編排，錯誤一律在此收斂，
  不讓例外冒進 Qt 事件圈拖垮主程式。
"""
